#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    double numero =5;
    double elevado = pow(numero, 2);
    cout << "El numero " << numero << " elevado al cuadrado es " << elevado;
    return 0;
}
